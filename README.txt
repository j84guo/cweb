Cweb is a lightweight multi-threaded HTTP server which servers static content. Inspired by IBM's nweb server.

Usage : gcc -o cweb cweb.c -lpthread // don't forget to link pthread
	./cweb port rootDirectory // you may need to sudo first 

